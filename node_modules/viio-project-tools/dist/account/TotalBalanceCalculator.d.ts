import { AxiosRequestConfig } from "axios";
export declare class TotalBalanceCalculator {
    private url;
    constructor(url: string);
    calculate(userId: number, asset: string, config?: AxiosRequestConfig): Promise<number>;
    calculateBalanceChange(transaction: any, asset: string): number;
    calculateApprovedOrCompletedBalanceChange(transaction: any, asset: string): number;
    calculateInTreasuryOrRefundBalanceChange(transaction: any, asset: string): number;
    getTotalValue(details: any, asset: string): number;
}
