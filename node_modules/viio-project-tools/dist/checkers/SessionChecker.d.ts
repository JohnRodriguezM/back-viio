import { APICheck, APICheckConfig, APICheckResult } from "itrm-tools";
import { Request } from "express";
export declare class SessionChecker implements APICheck {
    private sessionService;
    constructor(sessionService: string);
    getConfig(): APICheckConfig;
    apply(config: APICheckConfig, req: Request): Promise<APICheckResult>;
    private buildInvalidAuthenticationMethodRejection;
    private buildApproval;
    private buildUnexpectedErrorRejection;
}
