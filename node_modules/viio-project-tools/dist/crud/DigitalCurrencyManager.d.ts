import { AxiosRequestConfig } from "axios";
import { CrudServiceManager, StandardIdRequestDefinition } from "./CrudServiceManager";
export interface IAssetDetails {
    decimals: number;
    symbol?: string;
    chain?: string;
}
export interface IAsset {
    assetId?: number;
    name: any;
    ticker: string;
    type: string;
    details: IAssetDetails;
    config?: AxiosRequestConfig;
}
export interface IDigitalCurrencyDetails {
    chain: string;
    symbol: string;
    assetId: string;
    decimals: number;
    config?: AxiosRequestConfig;
}
export declare class DigitalCurrencyManager extends CrudServiceManager<IDigitalCurrencyDetails, IAsset> {
    private url;
    constructor(url: string);
    findById(id: StandardIdRequestDefinition): Promise<IAsset | undefined>;
    find(details: IDigitalCurrencyDetails): Promise<IAsset | undefined>;
    create(details: IDigitalCurrencyDetails): Promise<IAsset | undefined>;
    modify(asset: IAsset): Promise<any>;
    destroy(asset: IAsset): Promise<any>;
}
