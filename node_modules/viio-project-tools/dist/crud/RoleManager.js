"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RoleManager = void 0;
const CrudServiceManager_1 = require("./CrudServiceManager");
class RoleManager extends CrudServiceManager_1.CrudServiceManager {
    constructor(url) {
        super();
        this.url = url;
    }
    findById(id) {
        return this.sendFinding(`${this.url}/roles?id=${id.id}`, id.config);
    }
    find(details) {
        return this.sendFinding(`${this.url}/roles?role=${details.role}`, details.config);
    }
    create(details) {
        return this.sendCreation(`${this.url}/roles`, { role: details.role, permissions: details.permissions }, details.config);
    }
    modify(role) {
        return this.sendModification(`${this.url}/roles/${role.roleId}`, { role: role.role, permissions: role.permissions }, role.config);
    }
    destroy(role) {
        return this.sendDestuction(`${this.url}/roles/${role.roleId}`, role.config);
    }
}
exports.RoleManager = RoleManager;
//# sourceMappingURL=RoleManager.js.map