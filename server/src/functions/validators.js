/*const Joi = require('joi');

const schemacreateProduct = Joi.object({
  name: Joi.string().min(2).required(),
  price: Joi.number().max(5200).required(),
});

module.exports = schemacreateProduct;*/